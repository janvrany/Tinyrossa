Interface {
	#name : #LockableI,
	#category : #'Interfaces-Examples'
}

{ #category : #'acquire & release' }
LockableI >> acquire [
	"Acquire the lock"
]

{ #category : #'acquire & release' }
LockableI >> release [
	"Release the lock"
]
